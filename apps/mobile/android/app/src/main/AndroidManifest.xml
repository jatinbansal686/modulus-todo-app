<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <uses-permission android:name="android.permission.INTERNET" />

  <!--
    android:enableOnBackInvokedCallback="false" is deliberate, not an oversight.

    React Navigation does not yet support Android's predictive back gesture. With
    the callback enabled, the system drives the back animation itself and shows the
    *launcher* behind the app during the swipe, then React Navigation pops a screen
    anyway — so the user sees their home screen flash mid-gesture. Opting out keeps
    back on the legacy path, which React Navigation handles correctly.

    ⚠️ `react-native-bootsplash generate` rewrites this file and drops comments;
    re-add this note if it disappears. Revisit when React Navigation ships
    predictive-back support.
  -->
  <application
    android:name=".MainApplication"
    android:label="@string/app_name"
    android:icon="@mipmap/ic_launcher"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:allowBackup="false"
    android:theme="@style/AppTheme"
    android:usesCleartextTraffic="${usesCleartextTraffic}"
    android:enableOnBackInvokedCallback="false"
    android:supportsRtl="true">
    <activity
      android:name=".MainActivity"
      android:label="@string/app_name"
      android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
      android:launchMode="singleTask"
      android:windowSoftInputMode="adjustResize"
      android:exported="true"
      android:theme="@style/BootTheme">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
  </application>
</manifest>
